[pytest]
asyncio_mode = auto
markers =
    unit: pure logic tests, no external calls
    mocked: tests that mock HTTP calls
    integration: tests that call live services
filterwarnings =
    ignore::DeprecationWarning:fastapi_app
